# Server Configuration (for Node.js server)
PORT=3000

# Application Configuration (must be prefixed with VITE_)
VITE_APP_NAME=Pixology.ai
VITE_API_URL=https://api.pixology.ai

# Note: Do NOT set NODE_ENV in .env file
# Vite automatically sets it based on the command:
# - npm run dev -> NODE_ENV=development
# - npm run build -> NODE_ENV=production
# - npm run start -> Uses PORT for the server
